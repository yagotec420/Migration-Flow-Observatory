# Usado pelo Prisma CLI (migrate, generate, seed, studio) executado
# a partir da pasta database/. Deve ser IDÊNTICO ao DATABASE_URL usado
# pelo docker-compose e pelo backend (ver .env.example na raiz do projeto).
DATABASE_URL="postgresql://migration_user:CHANGE_ME@localhost:5432/migration_flow_observatory?schema=public"
