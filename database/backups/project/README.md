# project/

Destino de `migration-flow-complete-backup.zip`, gerado por `database/scripts/create-full-backup.sh`.

Este arquivo não é versionado por padrão (adicione `database/backups/project/*.zip` ao `.gitignore` do seu fork se preferir não versionar binários grandes no Git). Para times que preferem manter um histórico de backups completos versionado, use a convenção de pastas datadas em `database/backups/archive/` (ver `../README.md`).
